<view class="poster"><top-bar class="topBar" vue-id="5227b1f1-1" topBackgroundColor="{{topBackgroundColor}}" color="{{color}}" backImage="{{backImage}}" barName="{{barName}}" title="{{title}}" menuWidth="{{menuWidth}}" menuTop="{{menuTop}}" menuHeight="{{menuHeight}}" menuLeft="{{menuLeft}}" menuBottom="{{menuBottom}}"></top-bar></view>